import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Semerena-DEV_Ionic-App',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
